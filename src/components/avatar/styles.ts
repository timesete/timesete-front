import styled from "styled-components";

export const AvatarWrapper = styled.button`
  height: 3rem;
  width: 3rem;
  border-radius: 1.5rem;

  border: 0;

  background-color: yellow;
  filter: drop-shadow(1px 5px 10px rgba(0, 0, 0, 0.2));
`
